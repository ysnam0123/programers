do ... while
: 무조건 최소 한번 실행 후 조건 검사 후 실행
do{
  실행
  }
  while (조건)


  for....in : 객체의 속성을 반복. 키를 기준으로 반복, 배열에는 사용을 권장하지 않는다.
    변수 선언 = {}
    for(let 값 in 변수 선언){
          실행문
    }
  
  for....of : 배열 반복, 문자열 등의 요소를 반복할때 사용된다, 객체에서는 사용을 권장하지 않는다.
  
  배열선언 = []
  for (let 값 of 배열선언){
    실행문
  }

  forEach : 배열의 각 요소를 반복하며 실행할때 사용한다. 
  ***중간에 반복을 종료할 수 없다.
    배열.forEach(function(요소,인덱스,배열){
        실행문
    })

      요소 : 현재 반복중인 배열의 값, 인덱스 : 현재 요소의 위치,   배열 : 반복중인 원배열

  break : 반복문 즉시 종료
  for (초기값, 조건식, 증감값){
    조건 실행문
    break
  }
  
  continue : 현재 반복만 건너뛰고 다음 반복 실행
    for (초기값,조건식,증감값){
        조건 실행문
        continue;
    }

object.entries : [키,값],[키,값] 형태의 배열
object.keys : [키,키,키]
object.values: [값,값,값]
객체가 지니고 있는 값에 대하여 반복을 할 때는 함수 또는 for...in